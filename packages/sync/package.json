{
  "name": "@loopcut/sync",
  "version": "0.5.0",
  "description": "Sync Loopcut activity to structured daily summaries. Uses Claude Code CLI for AI extraction.",
  "type": "module",
  "bin": {
    "loopcut-sync": "dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "dev": "bun run src/index.ts",
    "prepublishOnly": "bun run build"
  },
  "keywords": [
    "loopcut",
    "productivity",
    "ai",
    "screen-recording",
    "daily-summary",
    "claude-code",
    "context-sync"
  ],
  "author": "Louis Beaumont <louis@screenpi.pe>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/loopcut/loopcut-desktop",
    "directory": "packages/sync"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
